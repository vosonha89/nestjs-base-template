/**
 * Enum representing standard exception codes used throughout the application.
 * @enum {string}
 */
export enum ExceptionCode {
	VALIDATION_ERROR = 'VALIDATION_ERROR',
	NOT_FOUND = 'NOT_FOUND',
	UNAUTHORIZED = 'UNAUTHORIZED',
	FORBIDDEN = 'FORBIDDEN',
	UNEXPECTED = 'UNEXPECTED'
}
